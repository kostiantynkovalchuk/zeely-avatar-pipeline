Two fixes needed. Do them sequentially, test after each.
Fix 1: Identity preservation — two-pass avatar generation
The current PuLID-only approach drifts identity on some faces (glasses disappear, hair color changes). Switch to a two-pass approach:
Pass 1 — Generate studio portrait with PuLID (already working, keep as-is)
Pass 2 — Face swap to restore original identity. After PuLID generates the studio portrait, run fal.ai's face-swap model to replace the generated face with the real face from the source photo. Use the model fal-ai/face-swap on fal.ai. The API takes:

base_image_url — the PuLID-generated studio portrait (the nice body/pose/lighting)
swap_image_url — the original user photo (the real face to restore)

This preserves PuLID's studio body/lighting/pose while locking the face to the real person. The result should be a studio portrait that is unmistakably the same person as the input photo — same glasses, same hair, same features.
Update generate_avatar() to:

Run PuLID (existing code)
Upload both the PuLID result and the original user photo to fal.ai CDN using fal_client.upload_file()
Run fal-ai/face-swap with the PuLID result as base and original photo as swap
Save the face-swapped result as the final avatar

Add retry logic to the face-swap step (same @with_retry pattern as other API calls).
Fix 2: Reduce garment text warping on IDM-VTON
The Gucci hoodie text warps badly (GUCCI → GUCCCI, FIRENZE → TRENZE). Three changes to reduce this:

Increase IDM-VTON inference steps from 30 to 50. Higher steps give the model more time to resolve fine details like text.
Update the garment_des parameter to explicitly describe the text on the garment. Instead of deriving it from the filename, hardcode awareness of text: change the garment description to include "Green Gucci Firenze 1921 hoodie with interlocking GG logo and red-navy braided stripe". The model uses this description to guide generation — being explicit about the text content helps preserve it.
Set seed to 0 (random) instead of fixed 42. Different seeds produce different text renderings — some will be better. We can cherry-pick the best result later, or run each user twice and keep the better one.

After both fixes, clear output/ and re-run all 4 users. Show me the batch_report.json and tell me the total fal.ai cost for this run.