Two fixes, then re-run.
Fix 1: Fix user_attributes.json — glasses are on wrong users. Replace the file contents with:
json{
  "001.webp": {"glasses": false},
  "002.jpg":  {"glasses": true,  "glasses_description": "round pink-tinted glasses"},
  "003.webp": {"glasses": false},
  "004.jpg":  {"glasses": true,  "glasses_description": "rectangular dark-framed glasses"}
}
Fix 2: Pre-process garment image to remove hood and laces before sending to IDM-VTON.
The hoodie drawstring laces cross directly over the "GUCCI" text. IDM-VTON treats the laces as part of the garment geometry and warps the text around them — this is why the "I" in GUCCI consistently separates or distorts, and why "FIRENZE" loses letters.
The fix: before sending the garment image to IDM-VTON, create a pre-processed version that removes the hood area and laces, leaving only the clean front panel with text.
Implementation:

Add a function preprocess_garment(garment_path) that runs before transfer_outfit():

Open the garment image
Detect the top of the text region: scan downward from the top of the image looking for the first row where white pixels appear (the "G" of GUCCI is white text on green). The text starts roughly 30-35% from the top of the garment image.
Crop the image to remove everything above the text start minus a small margin (keep 5% padding above the text). This removes the hood, drawstring laces, and collar area.
Also crop 5% from each side to remove any edge artifacts
Save to a temp file like input/outfits/.garment_preprocessed.png
Return the path to the preprocessed image


In transfer_outfit(), before calling _run_idm_vton(), run the garment through preprocess_garment() and pass the preprocessed version to IDM-VTON instead of the original.
The preprocessed garment should show: GUCCI text, GG interlocking logo, red-navy braided stripe, FIRENZE text, 1921 — but NO hood, NO drawstring laces, NO collar. Just the flat front panel of the hoodie from the text down.
Keep the original garment image untouched in input/outfits/. The preprocessing creates a temporary file that gets used only for the VTON API call.
Add a log line: "Preprocessed garment: cropped from {original_height} to {new_height}px (removed hood/laces)"

After both fixes, clear output/ and re-run python3 pipeline.py. Show me the output images and batch_report.json.
